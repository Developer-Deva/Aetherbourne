# Bridge Contracts (Pointer)

This document has been split into a shared, canonical contract:

- **Authoritative:** `docs/bridge_contracts.md`

Use that file as the single source of truth for event schema, emission/resolution order, emotion→behavior mapping, memory gating, personality drift ordering, and arbitration/time rules.

